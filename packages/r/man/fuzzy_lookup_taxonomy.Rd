% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fuzzy.R
\name{fuzzy_lookup_taxonomy}
\alias{fuzzy_lookup_taxonomy}
\title{Look up taxonomy for potentially misspelled species names}
\usage{
fuzzy_lookup_taxonomy(species)
}
\arguments{
\item{species}{A character vector of scientific names (possibly misspelled).}
}
\value{
A \code{data.frame} with the same columns as \code{lookup_taxonomy()}, plus
\code{input_name} (the original string) and \code{matched_name} (the GBIF canonical
name used for lookup, or \code{NA} when no correction was found and the
original name was used as-is).
}
\description{
Runs \code{correct_species_names()} first to obtain GBIF-canonical names, then
calls \code{lookup_taxonomy()} with the corrected names.  A warning is issued
listing any names that were auto-corrected.
}
\examples{
\dontrun{
fuzzy_lookup_taxonomy(c("Ballanus glandula", "Homo sapiens"))
}

}
